1.	Run CMD as administrator

type and run:	@powershell -NoProfile -ExecutionPolicy Bypass -Command "iex ((new-object net.webclient).DownloadString('https://chocolatey.org/install.ps1'))" && SET PATH=%PATH%;%ALLUSERSPROFILE%\chocolatey\bin

type and run: 	choco install pip (if error then go to systempropertiesadvanced/enviroment variables include C:\tools\python2\Scripts into "parth")
type and run: 	pip install mako
type and run: 	pip install pyyaml (yaml)

2.	Go to the website source from CMD then 

type and run:	python generate.py site.yaml
type and run: 	python generate.py site_en.yaml

3.	Download WINSCP install and fill in these details when asked

protocol:	ftp
host: 		[REDACTED]
username: 	[REDACTED]
password:	[REDACTED]

Note: 	Edit the site from input folder and generate the site with step 2 then upload the site by step 3!

Errors that may occure:
-you have python 3 or higher (or it is the future and you have python 4)
--you need python 2.7 and NOT use pip install ... but pip2.7 install